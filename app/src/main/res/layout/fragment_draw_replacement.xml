<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="#FFFFFF"
    android:gravity="top|center_horizontal">

    <!-- Top bar: back + title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/btnBackReplacement"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="8dp"
            android:src="@drawable/ic_back_button"
            android:contentDescription="Back" />

        <TextView
            android:id="@+id/tvReplacementTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Draw Replacement"
            android:textSize="22sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"
            android:textColor="#000000"
            android:paddingStart="8dp" />
    </LinearLayout>

    <!-- Main content -->
    <TextView
        android:id="@+id/tvReplacementSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select a replacement entrant from the waitlist."
        android:textSize="14sp"
        android:textColor="#666666"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/btnGenerateReplacement"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select replacement"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:background="@drawable/rounded_button_minimal"
        android:layout_marginTop="24dp" />

    <TextView
        android:id="@+id/tvReplacementResult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Replacement: (not drawn yet)"
        android:layout_marginTop="24dp"
        android:textSize="16sp"
        android:textColor="#333333" />

</LinearLayout>
