<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Title (mirrors Manage Images) -->
    <TextView
        android:id="@+id/AdminM_profiles_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Manage Profiles"
        android:textSize="24sp"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp"/>

    <!-- Search (mirrors Manage Images) -->
    <EditText
        android:id="@+id/Profil_search"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="search profile name...."
        android:inputType="text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/AdminM_profiles_title" />

    <!-- Add Button -->
    <Button
        android:id="@+id/Admin_ad_profile"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Add Profile"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnDeleteSelectedProfile"
        app:layout_constraintTop_toBottomOf="@id/Profil_search"
        app:layout_constraintHorizontalWeight="1" />

    <!-- Delete Button -->
    <Button
        android:id="@+id/btnDeleteSelectedProfile"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Delete Selected"
        app:layout_constraintTop_toBottomOf="@id/Profil_search"
        app:layout_constraintStart_toEndOf="@id/Admin_ad_profile"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontalWeight="1" />

    <!-- List -->
    <ListView
        android:id="@+id/Profile_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:choiceMode="singleChoice"
        app:layout_constraintTop_toBottomOf="@id/Admin_ad_profile"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
